# ホーム

## 目的 / 対応 Story

スタンプを押す起点。ラリーを一覧し、作成し、ワンタップで押す。収集数が分かり、直近のスタンプだけ確認する。全件はラリー詳細。

- S-001 ラリーを作成する
- S-002 ラリーにスタンプを押す
- S-008 ラリーの収集数を見る

## 入りと出

- 入り: タブ「ホーム」。ルート案 `/`
- 出:
  - 「ラリーを作る」→ [create-rally.md](./create-rally.md)
  - 「スタンプを押す」→ その場で付く。トーストから [add-stamp-memo.md](./add-stamp-memo.md)
  - ラリー行タップ → [rally-detail.md](./rally-detail.md)
  - 直下のスタンプ行タップ → [stamp-detail.md](./stamp-detail.md)
- 削除: 確認ダイアログのまま（S-001）。ラリー行の中。

## レイアウト

現状: 中央寄せ。ラリー名、タイプ、「スタンプを押す」、直下に全スタンプ（日時 + メモ）、削除。収集数なし。詳細へ進めない。

目標:

```
ホーム                         [ラリーを作る]

┌─────────────────────────────────┐
│ 今年会った研究者          人    │
│ 12                             │  ← 収集数（目標ありなら 12 / 20）
│ [スタンプを押す]                │
│ 9月20日 11:40  メモがあれば一行 │  ← 新しい順。最新数件まで
│ 9月18日 19:02                   │
│                          削除   │
└─────────────────────────────────┘

（同様に次のラリー）
```

- ラリー行本体のタップは詳細。ボタン（押す / 削除）は行タップと分けて取る。
- 直下は最新数件。それ以上は詳細へ。件数の上限は実装時に決めてよい（目安 3）。

## 出すデータ

- ラリー: 名称、タイプ（人 / 場所 / 行動）
- 収集数。目標があるときだけ `現在 / 目標`
- 直近スタンプ: 日時、メモがあれば一行（全文は詳細）

## 空 / 読込 / エラー

- 空: ラリーが無い。「ラリーを作る」だけ見える
- 読込: 一覧が出るまで待つ。スケルトンは必須にしない
- エラー: 「読み込めませんでした」+ 再試行（現状どおり）

## 未決事項

- 直下に出す件数の確定値
- 目標達成時の見た目（E-003 で必要なら stamp-detail / rally-detail 側で足す。ホームは数の表示まで）
