# Backlog

Single source of truth for Epic / Story / Task / Sub. Lightweight agile only — no sprints, velocity, or points.

プロダクト定義（概要・ビジョン・基盤）は [product.md](./product.md) を参照する。

## How to use

1. Add or refine a **Story** before implementation. Link it under an **Epic**.
2. Split the Story into **Tasks**, then each Task into **Subs** (user-confirmable steps, not layers).
3. Deliver **one Story** at a time. Mark Sub checkboxes as you commit (**1 Sub = 1 commit**).
4. IDs: `E-###`, `S-###`, `T-###`, `ST-###`. Reference Story + Sub IDs in commit messages.

Template (copy under an epic):

```markdown
## Epic: E-001 Title

ゴール:

### Story: S-001 Title

As a ... I want ... so that ...

受け入れ:

- Given ... When ... Then ...

Tasks:

- [ ] T-001: （振る舞いのまとまり）
  - [ ] ST-001: （ユーザーが確認できる一歩）
  - [ ] ST-002: （次の一歩）
```

---

<!-- Product epics and stories go below. Keep this file the only backlog. -->

## Epic: E-001 日常の体験をスタンプとして記録する

ゴール: ユーザーがその場で起きた出来事を最小限の操作で記録できるようにする。対象は人・場所・行動。「記録を残すこと」を最優先とし、ラリーへの分類は記録完了の必須条件としない。

### Story: S-001 人を記録する

As a 日常の体験を残したいユーザー
I want 会った人をすぐにスタンプとして記録したい
so that 誰と会ったかを忘れないうちに残しておきたい

受け入れ:

- Given 記録を開始している When 「人」として保存する Then 人スタンプが作成される
- Given 相手の名前を入力した When 保存する Then 誰に会ったかが保持される
- Given スタンプを保存した When 記録を確認する Then 記録日時が保持されている
- Given 補足情報を入力した When 保存する Then 補足情報が保持される
- Given ラリーを選んでいない When 保存する Then 記録を完了できる

Tasks:

- [ ] T-001: 単一の記録入口から、名前を入れて人スタンプを保存できる
  - [ ] ST-001: ホームに「記録する」入口があり、記録画面を開ける（ルート `index` + `record` formSheet。永続化なし）
  - [ ] ST-002: 記録画面で種類「人 / 場所 / 行動」を出し、場所・行動は選べない。人を選ぶと名前入力が出る
  - [ ] ST-003: 名前を入れて保存すると人スタンプが残り、元ページに戻って名前が反映される（SQLite + Query はこの Sub で初めて入れる。ラリー UI なし）
  - [ ] ST-004: 名前が空（または空白のみ）のときは保存できない
- [ ] T-002: 保存したスタンプの記録日時を確認できる
  - [ ] ST-005: 保存後の元ページ結果に、自動付与された記録日時が表示される（手入力なし）
- [ ] T-003: 任意の補足メモをアコーディオンから保存できる
  - [ ] ST-006: アコーディオンからメモを入れて保存すると、戻った結果にメモが反映される
  - [ ] ST-007: アコーディオンを開かない / メモ空でも、名前があれば保存できる

### Story: S-002 場所を記録する

As a 日常の体験を残したいユーザー
I want 訪れた場所をスタンプとして記録したい
so that 自分がどこへ行ったかを蓄積して振り返りたい

受け入れ:

- Given 記録を開始している When 「場所」として保存する Then 場所スタンプが作成される
- Given 場所の名称を入力した When 保存する Then 場所名が保持される
- Given スタンプを保存した When 記録を確認する Then 記録日時が保持されている
- Given 位置情報が利用可能なとき When 保存する Then 位置情報を記録できる
- Given 位置情報が利用できないとき When 保存する Then 記録を作成できる

Tasks:

- （実装着手時に垂直スライスで追加）

### Story: S-003 行動を記録する

As a 日常の体験を残したいユーザー
I want 自分が行ったことをスタンプとして記録したい
so that 場所や人物に限定されない日々の体験も残したい

受け入れ:

- Given 記録を開始している When 「行動」として保存する Then 行動スタンプが作成される
- Given 何をしたかを入力した When 保存する Then 行動内容が保持される
- Given スタンプを保存した When 記録を確認する Then 記録日時が保持されている
- Given 場所を入力していない When 保存する Then 記録を作成できる

Tasks:

- （実装着手時に垂直スライスで追加）

### Story: S-004 記録時の入力を少なくする

As a 日常の体験を残したいユーザー
I want できるだけ少ない入力でスタンプを作りたい
so that 記録作業そのものを負担にしたくない

受け入れ:

- Given 主要情報だけを入力した When 保存する Then スタンプを保存できる
- Given 日時など自動取得できる情報がある When 保存する Then それらの情報が自動的に補完される
- Given 任意情報を入力していない When 保存する Then スタンプを保存できる

Tasks:

- （実装着手時に垂直スライスで追加）

### Story: S-005 記録に補足情報を残す

As a 日常の体験を残したいユーザー
I want 必要なときだけ記録に補足情報を加えたい
so that 後で見返したときに当時の状況を思い出したい

受け入れ:

- Given スタンプを作成または編集している When メモを追加する Then スタンプにメモが残る
- Given 将来の拡張として When データ構造を定義する Then 写真等の証跡を追加できる構造とする
- Given 補足情報を入力していない When 保存する Then 記録を完了できる

注記: 作成時の任意メモ（アコーディオン）は S-001 で扱う。本 Story は写真等の証跡を足せる構造、および作成時以外の補足を対象とする。

Tasks:

- （実装着手時に垂直スライスで追加）

---

## Epic: E-002 過去の記録を振り返る

ゴール: ユーザーがこれまでに記録したスタンプを確認できるようにする。時系列や人・場所・行動の観点から振り返れることを目的とする。

### Story: S-006 記録を一覧で見る

As a 日常の体験を残したいユーザー
I want これまでのスタンプを一覧で見たい
so that 自分が最近何をしていたかを簡単に振り返りたい

受け入れ:

- Given スタンプが作成されている When 一覧を開く Then 作成したスタンプを確認できる
- Given 人・場所・行動のスタンプがある When 一覧を見る Then 種類の違いを識別できる
- Given 複数のスタンプがある When 一覧をたどる Then 新しい記録から過去の記録へ確認できる

Tasks:

- （実装着手時に垂直スライスで追加）

### Story: S-007 種類ごとに記録を見る

As a 日常の体験を残したいユーザー
I want 人・場所・行動ごとに記録を見たい
so that 目的に応じて自分の記録を探しやすくしたい

受け入れ:

- Given 人のスタンプがある When 人で絞り込む Then 人の記録を確認できる
- Given 場所のスタンプがある When 場所で絞り込む Then 場所の記録を確認できる
- Given 行動のスタンプがある When 行動で絞り込む Then 行動の記録を確認できる
- Given 複数種類のスタンプがある When 全種類を選ぶ Then まとめて確認できる

Tasks:

- （実装着手時に垂直スライスで追加）

### Story: S-008 スタンプの詳細を見る

As a 日常の体験を残したいユーザー
I want 過去に作ったスタンプの詳細を見たい
so that そのときに残した情報を改めて確認したい

受け入れ:

- Given スタンプが存在する When 詳細を開く Then スタンプの内容を確認できる
- Given スタンプが存在する When 詳細を開く Then 日時を確認できる
- Given 補足情報が記録されている When 詳細を開く Then 補足情報を確認できる
- Given ラリーに所属している When 詳細を開く Then 所属しているラリーを確認できる

Tasks:

- （実装着手時に垂直スライスで追加）

---

## Epic: E-003 記録をラリーとしてまとめる

ゴール: ユーザーが複数のスタンプを任意のテーマでまとめ、自分自身の収集・達成体験を作れるようにする。ラリーの内容や目的はアプリ側で固定しない。

### Story: S-009 ラリーを作る

As a 日常の体験を残したいユーザー
I want 自分だけのラリーを作りたい
so that 自分が集めたい体験や達成したいテーマを自由に決めたい

受け入れ:

- Given ラリー作成を開始している When 保存する Then 新しいラリーが作成される
- Given 名称を入力した When 保存する Then ラリーに名称が付く
- Given テーマをユーザーが決めた When 保存する Then そのテーマでラリーを作成できる

Tasks:

- （実装着手時に垂直スライスで追加）

### Story: S-010 スタンプをラリーに追加する

As a 日常の体験を残したいユーザー
I want 記録したスタンプをラリーに追加したい
so that 自分の体験をテーマ別に収集したい

受け入れ:

- Given 既存のスタンプとラリーがある When スタンプをラリーに追加する Then スタンプがそのラリーに所属する
- Given スタンプを作成した直後 When ラリーへ追加する Then 追加できる
- Given 過去に作成したスタンプがある When 後からラリーへ追加する Then 追加できる

Tasks:

- （実装着手時に垂直スライスで追加）

### Story: S-011 1つのスタンプを複数ラリーで使う

As a 日常の体験を残したいユーザー
I want 1つのスタンプを複数のラリーに入れたい
so that 1つの体験が複数の意味を持つことに対応したい

受け入れ:

- Given 1つのスタンプと複数のラリーがある When 同じスタンプを複数ラリーに追加する Then すべての対象ラリーに所属する
- Given スタンプが複数ラリーに所属している When いずれかのラリーを確認する Then そのスタンプが含まれている

Tasks:

- （実装着手時に垂直スライスで追加）

### Story: S-012 ラリーの内容を見る

As a 日常の体験を残したいユーザー
I want ラリーに集めたスタンプを一覧で見たい
so that そのテーマでこれまで何を達成したかを確認したい

受け入れ:

- Given ラリーにスタンプが所属している When ラリー詳細を開く Then 所属するスタンプを確認できる
- Given 所属するスタンプがある When 一覧を見る Then 各スタンプの概要を確認できる
- Given 所属するスタンプがある When スタンプを選ぶ Then スタンプの詳細へ移動できる

Tasks:

- （実装着手時に垂直スライスで追加）

---

## Epic: E-004 ラリーの進捗を楽しむ

ゴール: ユーザーが自分のラリーにどれだけスタンプが集まったかを確認できるようにする。「集めている」「進んでいる」という感覚を与えることを目的とする。

### Story: S-013 ラリーの収集数を見る

As a 日常の体験を残したいユーザー
I want ラリーにいくつスタンプが集まったかを知りたい
so that 自分の活動が積み上がっていることを実感したい

受け入れ:

- Given ラリーにスタンプがある When ラリーを確認する Then スタンプ数を確認できる
- Given 複数のラリーがある When ラリー一覧を開く Then 進捗の概要を確認できる

Tasks:

- （実装着手時に垂直スライスで追加）

### Story: S-014 目標に対する進捗を見る

As a 日常の体験を残したいユーザー
I want 目標数を設定したラリーの達成状況を見たい
so that 目標までどれくらい進んだかを楽しみたい

受け入れ:

- Given 目標数が設定されたラリーがある When 進捗を確認する Then 現在数 / 目標数を確認できる
- Given 目標数が設定されていないラリーがある When 進捗を確認する Then 目標なしのラリーとしても扱える

Tasks:

- （実装着手時に垂直スライスで追加）

---

## Epic: E-005 カレンダーから日常を振り返る

ゴール: ユーザーがいつ何をしていたかをカレンダーから振り返れるようにする。記録件数や内容を日付単位で確認できるようにする。

### Story: S-015 記録がある日をカレンダーで見る

As a 日常の体験を残したいユーザー
I want どの日にスタンプを記録したかをカレンダーで確認したい
so that 自分の活動の流れを時間軸で振り返りたい

受け入れ:

- Given カレンダーを開いている When 月を表示する Then 月単位で日付を確認できる
- Given ある日にスタンプがある When カレンダーを見る Then 記録が存在する日を識別できる

Tasks:

- （実装着手時に垂直スライスで追加）

### Story: S-016 特定の日の記録を見る

As a 日常の体験を残したいユーザー
I want カレンダーの日付を選択して、その日の記録を見たい
so that その日に何をしていたかを思い出したい

受け入れ:

- Given カレンダーを開いている When 日付を選ぶ Then その日を選択できる
- Given 選択した日にスタンプがある When 日詳細を開く Then その日のスタンプを確認できる
- Given 同じ日に人・場所・行動がある When 日詳細を開く Then 同じ日付内で種類を確認できる

Tasks:

- （実装着手時に垂直スライスで追加）

---

## Epic: E-006 記録内容を後から修正・整理する

ゴール: 記録時の入力負荷を下げ、必要な整理や修正は後から行えるようにする。誤記録の修正や、後からのメモ・ラリー追加にも対応する。

### Story: S-017 スタンプを編集する

As a 日常の体験を残したいユーザー
I want 作成済みのスタンプを後から編集したい
so that 入力間違いを修正したり、後から情報を補足したい

受け入れ:

- Given 作成済みのスタンプがある When 主要情報を編集して保存する Then 変更が反映される
- Given 作成済みのスタンプがある When 任意情報を追加・変更して保存する Then 変更が反映される

Tasks:

- （実装着手時に垂直スライスで追加）

### Story: S-018 ラリーへの所属を変更する

As a 日常の体験を残したいユーザー
I want スタンプが所属するラリーを後から変更したい
so that 記録時に整理方法を決められない場合にも対応したい

受け入れ:

- Given スタンプとラリーがある When 後からラリーへ追加する Then スタンプがそのラリーに所属する
- Given スタンプがラリーに所属している When ラリーから外す Then そのラリーから外れる
- Given ラリーからスタンプを外した When スタンプを確認する Then 元のスタンプ自体は失われていない

Tasks:

- （実装着手時に垂直スライスで追加）

### Story: S-019 不要な記録を削除する

As a 日常の体験を残したいユーザー
I want 不要になったスタンプを削除したい
so that 誤って作成した記録を残したくない

受け入れ:

- Given 自分のスタンプがある When 削除を実行する Then スタンプを削除できる
- Given 削除を開始した When 確認なしに即削除しようとしても Then 意図しない削除を防ぐ手段がある

Tasks:

- （実装着手時に垂直スライスで追加）
